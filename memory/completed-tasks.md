# Completed Tasks — <project>

> The ✅ **done-archive**. The planner moves a task here from `planning/progress.md` on merge. Read at
> wrap/audit, never at task start. Newest last; keep the stable `#N` ID.
> *(Small projects may instead keep ✅ inline in `progress.md` and delete this file — but then also drop the
> `../memory/completed-tasks.md` references in `planning/todo.md`/`progress.md` so no route dangles.)*

- ✅ **#<N>** <task title> — merged <YYYY-MM-DD>, PR #<n>. <one-line what shipped → `decisions.md` for rationale>
